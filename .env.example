# Supabase — Project URL + anon public key (Settings → API). Never use service_role in VITE_*.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Gemini — required for J·OS AI features (Chat, Goals, Daily Focus, etc.)
# Model is fixed in code: gemini-2.0-flash
VITE_GEMINI_API_KEY=
